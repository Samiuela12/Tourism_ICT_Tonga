[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && python -c 'from app import init_db; init_db()'"]

[start]
cmd = "cd backend && gunicorn app:app"
